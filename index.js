const myName = "Noor Athamny";
const age = 25;

console.log(`My name is ${myName} and i am ${age} years old..`);

const length = 5;
const width = 3;

console.log(
  `the area of a rectangle with length of ${length} and width of ${width} is ${
    length * width
  }. `
);

const day = "Monday";
const task1 = "Learn javascript";
const task2 = "Read a book";
const task3 = "try so stay a live.";

console.log(`On ${day} i will ${task1} and ${task2} and ${task3}`);

const myPets = 3;

console.log(`i have ${myPets} pets.`);

const triangleBase = 10;
const trianleHeight = 5;

console.log(
  `The area of a the triangle with base of ${triangleBase} and height of ${trianleHeight} is ${
    (triangleBase * trianleHeight) / 2
  }`
);
